#include <string.h>
#include <mem.h>
#include <stdio.h>
#include <conio.h>
#include <iostream.h>
#include <dos.h>

/*void nowy()
{
int k,l,i,l1,l11,wst;
char w[72],z;
wst=0;
clrscr();
 printf("Wsp¢ˆrzedne miejsca wstawienia:\n");
 do{
 cout << " Numer kolumny: "; cin >> k;
 }while(k<0 || k>72);
 do{
 cout << " Numer wiersza: "; cin >>l;
 }while(l<0 || l>25);
clrscr();

gotoxy(k,l); printf("Edytuj >");
gotoxy(k+8,l);
for (i=1;i<=72-k;i++)
{
z=getch();
l1=i;
l11=1;

if (z==47)
{
gotoxy(5,25);
printf("Znak zabroniony");

/*do
{
gotoxy(5,25);
printf("Znak zabroniony");
}
while (z!=47);

//l11=l1;
gotoxy(5,25);
delay(1000);
printf("                   ");
gotoxy(k+l1+7,l);

i=i--;
}
if  (z==75)
{

 gotoxy(k+8+i-l11,l);
}
else
{
w[i]=z; printf("%c",w[i]);


}
}

gotoxy(5,25);
printf("Koniec wiersza!");
getch();
}
char znak(char z)
{
100, 101, 102
};*/

void nowy()
{
int k,l,i,l1,l11,wst, koniec=0;
char z;
k=10; l=5;
z=getch();
if (koniec=0)
{
switch (z)
 {
 case 27:printf("pies"); break;
 default: gotoxy(k+1,l); printf("%c",z); break;



}
}
while (koniec=0);
}

 main()
{
clrscr();
nowy();
getch();
}